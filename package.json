{
  "name": "seattle-indies-expo-search",
  "homepage": "https://constancecchen.github.io/seattle-indies-expo-search/",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@elastic/react-search-ui": "^1.7.0",
    "@elastic/search-ui-app-search-connector": "^1.7.0",
    "gh-pages": "^3.2.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-modal": "^3.14.3",
    "react-scripts": "4.0.3"
  },
  "devDependencies": {
    "node-sass": "^6.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn build && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
